$("#unlike_<%= @post.id %>").addClass('glyphicon-heart-empty').removeClass('glyphicon-heart');
$("#likes_<%= @post.id %>").html("<%= j (render partial:'posts/likes', locals: { post: @post} ) %>");
$("#unlike_<%= @post.id %>").attr('href',"<%= like_post_path(@post.id)%>");
$("#unlike_<%= @post.id %>").attr("id","like_<%= @post.id %>");